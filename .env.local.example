# Required Configuration
# ----------------------

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/caelus"

# Authentication (Required)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-strong-32-char-secret

# OpenAI API (Required for AI features)
NEXT_PUBLIC_OPENAI_API_KEY=your-openai-api-key

# Feature Flags
NEXT_PUBLIC_ENABLE_AI_FEATURES=true
NEXT_PUBLIC_ENABLE_MARKETPLACE=true
NEXT_PUBLIC_ENABLE_BOUNTIES=true

# Quick Start Guide
# ----------------------
# 1. Copy this file to .env.local
# 2. Replace the DATABASE_URL with your PostgreSQL connection string
# 3. Generate a NEXTAUTH_SECRET (32+ random characters)
# 4. Add your OpenAI API key if you want to use AI features
# 5. Adjust feature flags as needed

# Generate a secure secret:
# - Linux/macOS: `openssl rand -base64 32`
# - Windows: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`

# For development, you can use a local PostgreSQL instance:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/caelus"

# Note: Keep your .env.local file secure and never commit it to version control